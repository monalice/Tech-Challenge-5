[project]
name = "tech-challenge-5"
version = "0.1.0"
dependencies = [
  "mlflow>=2.22,<3",
  "psycopg2-binary>=2.9",
  "SQLAlchemy>=2,<3",
  "boto3>=1.34.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [
  ".git",
  ".venv",
  "venv",
  "build",
  "dist",
  "__pycache__",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "C4", "N"]
ignore = ["N803", "N806"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.12"
explicit_package_bases = true
strict = true
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_unreachable = true
show_error_codes = true
pretty = true
exclude = [
  "^\\.venv/",
  "^venv/",
  "^build/",
  "^dist/",
]

[[tool.mypy.overrides]]
module = [
  "mlflow.*",
  "boto3.*",
  "joblib",
  "pandas",
  "psutil",
  "requests",
  "sklearn.*",
  "tensorflow.*",
  "langchain.*",
  "langfuse.*",
  "presidio_analyzer.*",
  "presidio_anonymizer.*",
  "evidently.*",
  "yfinance",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = [
  "langchain_openai.*",
]
ignore_errors = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov --cov-config=.coveragerc --cov-report=term-missing -q"
filterwarnings = [
  "ignore:'parseString' deprecated - use 'parse_string':pyparsing.exceptions.PyparsingDeprecationWarning",
  "ignore:'resetCache' deprecated - use 'reset_cache':pyparsing.exceptions.PyparsingDeprecationWarning",
  "ignore:'enablePackrat' deprecated - use 'enable_packrat':pyparsing.exceptions.PyparsingDeprecationWarning",
  "ignore:The behavior of DataFrame concatenation with empty or all-NA entries is deprecated:FutureWarning",
  "ignore:The 'app' shortcut is now deprecated:DeprecationWarning",
  "ignore:The filesystem tracking backend.*is deprecated.*:FutureWarning",
  "ignore:The filesystem model registry backend.*is deprecated.*:FutureWarning",
]
markers = [
  "mlflow_integration: optional integration test that spins up local MLflow container",
]
